# 고객사 데이터 반영 계획

## 현재 저장한 기본 데이터

- 원본 파일: `C:\Users\SFN\Downloads\고객사정보현황_20260604.xlsx`
- 변환 파일: `customer-master-20260604.json`
- 저장 건수: 15,592건
- 주요 필드:
  - `customerCode`: 고객코드
  - `customerName`: 고객명/매장명
  - `headquartersCode`: 본사코드
  - `logisticsCenter`: 물류센터
  - `postalCode`: 우편번호
  - `address`: 주소

## 앱에서 쓰는 방식

1. 앱 안에 마지막으로 검증된 고객 마스터 JSON을 기본 데이터로 포함한다.
2. 전표 OCR에서 매장명이 나오면 고객 마스터에서 매장명을 검색한다.
3. 검색 결과에서 사용자가 매장을 확인하면 해당 `customerCode`를 리포트 데이터에 붙인다.
4. 호차/착순은 사용자가 직접 확인하거나, 별도 안정된 데이터가 생기면 나중에 보조 정보로 붙인다.
5. 운영지도는 현재 이슈가 많으므로 기본 검색 흐름에서 제외한다.

## 신규 지점 추가 대응

신규 지점은 계속 생기므로 앱에 데이터를 고정하면 안 된다. 아래 방식으로 운영한다.

1. 기준 데이터는 GitHub/Render 서버에 둔다.
   - 예: `/data/customer-master-latest.json`
   - 날짜별 백업: `/data/customer-master-20260604.json`
2. 앱 실행 시 서버의 최신 고객 마스터 버전을 확인한다.
3. 최신 버전이 있으면 내려받아 iPhone 로컬 저장소에 캐시한다.
4. 인터넷이 안 되거나 서버가 실패하면 앱에 포함된 기본 데이터 또는 마지막 캐시 데이터로 검색한다.
5. 화면에는 `고객 데이터 기준일`을 작게 표시한다.
6. 검색이 안 되는 신규 지점은 앱에서 임시 고객으로 추가 저장하고, 관리자가 검수 후 다음 고객 마스터에 반영한다.

## 업데이트 작업 흐름

1. 새 엑셀 파일을 받는다.
2. 엑셀을 JSON으로 변환한다.
3. `customer-master-latest.json`을 새 파일로 교체한다.
4. GitHub에 커밋한다.
5. Render가 자동 배포되면 앱은 다음 실행 때 새 데이터를 받는다.

## 검색 우선순위

1. 고객코드 정확히 일치
2. 매장명 정확히 일치
3. 공백/기호 제거 후 매장명 포함 검색
4. 주소 포함 검색
5. 검색 결과가 없으면 앱에서 신규 지점 직접 추가
6. 후보가 여러 개면 사용자가 직접 선택

## 주의

- 전표의 숫자 `code`는 최종 고객코드가 아니다.
- 최종 고객코드는 고객 마스터에서 가져온 `S`, `B` 등으로 시작하는 고객코드다.
- 전표 OCR 결과는 자동 확정하지 말고, 매장 후보 선택 화면을 반드시 거친다.
