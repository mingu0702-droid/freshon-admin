# Freshon Admin 작업 메모

이 파일은 운영지도/WMS/프레시온 직접조회가 계속 커지는 동안 놓치기 쉬운 변수를 미리 정리해두기 위한 기준 문서입니다.

## 1. 일일동선 직접조회

목표:
- 프레시온 일일배차관리에서 선택 날짜/센터/호차의 실제 착지를 가져와 지도에 표시한다.
- 프레시온 응답은 성공했지만 지도 매칭이 실패하면 실패 원문을 바로 화면에 보여준다.

확인할 변수:
- 프레시온 API가 호차 필터를 실제로 적용했는지
- 응답 row 안의 호차 컬럼명이 무엇인지
- 고객코드 컬럼명이 무엇인지
- 고객명/주소 컬럼명이 무엇인지
- 주소가 상세주소와 분리되어 내려오는지
- 같은 고객코드가 운영지도 데이터에 없는 신규/이전신규 착지인지

다음 보강:
- 매칭 실패 원문에서 새 컬럼명을 발견하면 `buildStopFromFreshonDailyRow`의 후보 키에 추가한다.
- 실패 착지 중 고객코드는 있는데 운영지도에 없으면 "신규/운영지도 미등록"으로 분류한다.
- 주소만 있는 착지는 카카오 지오코딩 성공/실패를 따로 표시한다.

## 2. WMS 배차 분석

목표:
- 배차계획조정 상세 파일과 차량톤수 요약 파일을 분리해서 읽는다.
- 상세 파일은 착지/금액/고객정보 기준, 요약 파일은 톤수/호차별 총액 기준으로 사용한다.
- 호차 클릭 시 전체 착지와 용차 후보 묶음을 같이 본다.

확인할 변수:
- 파일 2개가 같은 날짜/센터 자료인지
- 상세 파일과 요약 파일의 호차 표기가 같은지
- 요약 파일의 차량톤수가 상세 파일 금액과 합연산되지 않는지
- 1톤/1.2톤/1.4톤/2.5톤/3.5톤 기준금액이 정확히 적용되는지
- 금액 기준 초과와 동선상 튀는 지역이 서로 다른 판단인지
- 사용자가 후보를 클릭했을 때 지도에는 해당 착지만 남는지

기준금액:
- 1.0톤: 6,100,000원
- 1.2톤: 9,600,000원
- 1.4톤: 10,500,000원
- 2.5톤: 13,600,000원
- 3.5톤: 14,600,000원

다음 보강:
- "초과" 호차는 기준 초과 금액을 먼저 보여준다.
- 용차 후보는 인근 호차에 붙이는 방식이 아니라, 튀는 지역구/고액 묶음을 새 용차 후보로 묶는다.
- 후보 사유는 "고액", "권역 외곽", "같은 구/동 묶음", "일일동선과 반대방향"으로 분리한다.

## 3. 신규권역 판단

목표:
- 주소 X열, 고객정보 Y열을 대량 붙여넣고 O/X/검토필요를 바로 판단한다.
- 결과는 엑셀에 그대로 붙여넣기 쉬운 값으로 출력한다.

확인할 변수:
- 아파트 동/호수 판별: `101동 101호`, `101동 101`, `106-1703`
- 상가 건물동은 아파트로 오판하지 않는지
- 제주도는 무조건 X, 사유 제주도
- 영남권/호남권/제주도 외 센터/권역은 공란
- 거리 km는 별도 열로만 표시하고 사유에는 넣지 않는다.

출력 기준:
- 판단(BV): `O`, `X`, `검토필요`
- 사유(BW): `아파트`, `배송동선 맞지않음`, `검토필요`, 공란
- 적용일자(BX): 사용자가 입력/선택한 날짜
- 센터/권역(BY): `영남권`, `호남권`, `제주도`, 공란
- 거리km: 내부 확인용 별도 열

## 4. UI/화면 구조

목표:
- 일일동선, WMS, 관리자 탭은 상단에서 고정 크기로 움직인다.
- WMS는 작업 화면이라 지도보다 표가 우선이다.
- 메인 지도는 유지하고, WMS 지도는 미니뷰로 둔다.

확인할 변수:
- WMS 탭 전환 시 상단 탭 너비가 튀지 않는지
- 지도 리사이즈 손잡이가 사용자가 잡을 수 있게 보이는지
- WMS 지도 오른쪽 손잡이처럼 불필요한 UI가 작업을 가리지 않는지
- 헤더 정렬/필터가 표 안에 자연스럽게 들어가는지
- 좌측 차량 리스트와 우측 착지 상세가 스크롤 독립인지

다음 보강:
- WMS 지도는 기본 미니뷰, 필요할 때만 확대한다.
- 차량 리스트 헤더에서 `호차`, `톤수`, `금액`, `사용률`, `착지`, `상태` 정렬/필터를 직접 조작한다.
- 상세 착지 리스트에는 고객코드/고객명/주소/금액/판단사유를 기본 표시한다.

## 5. Render/GitHub 캐시

목표:
- Render 무료/유료 환경에서도 서버 재배포 후 데이터가 최대한 유지되도록 한다.
- GitHub 캐시는 충돌해도 서버가 죽지 않게 한다.

확인할 변수:
- `GITHUB_TOKEN` 권한이 contents write 가능한지
- `GITHUB_CACHE_REPO`가 정확한지
- GitHub cache 파일 업데이트 시 409 충돌이 나는지
- Render가 `SIGTERM`을 정상 배포 교체로 보내는지, 실제 crash인지
- `FRESHON_COOKIE` 만료 여부

다음 보강:
- GitHub 캐시 쓰기는 최신 SHA 재조회 후 재시도한다.
- 실패해도 서버는 계속 살아있게 한다.
- Render 시작은 `npm start`가 아니라 `node src/server.js`로 한다.
- 배포 실패 시 `SIGTERM` 위쪽 로그를 우선 확인한다.

## 6. 앞으로 작업 우선순위

1. 일일동선 실패 착지 원문을 보고 프레시온 컬럼명 보강
2. WMS 화면을 사수 지도처럼 표 중심으로 더 압축
3. 용차 후보 묶음 로직 개선
4. 신규권역 결과 복붙 포맷 최종 고정
5. 프레시온 쿠키 갱신/조회 실패 안내 개선
6. 센터별 매출/배차현황 자동 연결 검토

